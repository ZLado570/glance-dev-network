gdn: 1
id: close-approach
version: 0.1.0
name: Close Approach
author: Markusraym
description: The asteroid passing closest to Earth today. Distance in lunar distances, size, speed, and a hazard warning if NASA has flagged it.
category: Science
entry: app.star
width: 192
height: 32
refresh: 3600
pages: [neo]
inputs:
  - key: apikey
    type: string
    app_input_type: api-key
    label: NASA API key (api.nasa.gov)
    default: "DEMO_KEY"
    help: "Works as-is on NASA's shared DEMO_KEY, which is rate-limited across everyone using it. Your own free key is instant and lifts that: https://api.nasa.gov (no account, just an email)."